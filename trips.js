// trips.js — placeholder
import { t } from "./i18n.js";
export function renderTrips(container) {
  container.innerHTML = `<div class="coming-soon"><h3>${t("tab_trips")}</h3><p>🚧 U izradi...</p></div>`;
}
